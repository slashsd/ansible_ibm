- name: HCP
  hosts: all
  tasks:
  - name: Apply a terraform plan
    community.general.terraform:
      state: present
      workspace: "slash_ws_ibm"
    register: tf_plan

  - name: Output the TF plan
    debug:
      var: tf_plan
